set expandtab

set tabstop=4
set shiftwidth=4
set expandtab

set background=dark

set number "all files now display line numbers
set hlsearch "all searches now highlight item searched for

filetype plugin on

syntax on

filetype indent plugin on
